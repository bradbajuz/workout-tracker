<%= form_for workout, html: {id: 'workout_form', class: 'col-xl-9'} do |f| %>
  <%= f.label :name, 'Workout Name' %>
  <%= f.text_field :name, class: 'form-control' %>
  <%= f.fields_for :exercise_sets do |builder| %>
    <%= render 'exercise_set_fields', f: builder %>
  <% end %>
<% end %>
